# Example env file for OceanX backend
# Copy this to `server/.env` and populate sensitive values locally.

# Node / server
NODE_ENV=development
PORT=3001

# Ethereum / RPC
# RPC URL for the network you will test on (e.g. Sepolia/alchemy or Infura endpoint)
# e.g. https://sepolia.infura.io/v3/<INFURA_KEY>
RPC_URL=https://your-sepolia-or-rpc-endpoint.example
CHAIN_ID=11155111

# OCX Token contract address (the deployed OCXToken on the target network)
TOKEN_CONTRACT_ADDRESS=0x3282E5D599b2A19c59C2c89EC9BDe5b5ad0F257E

# Backend signer (must match the authorized signer used when deploying the token contract)
BACKEND_SIGNER_ADDRESS=0x14Ac0ceB3fF8858358b487F6A24553fa3a04407b

# Backend private key (used to sign EIP-712 claim typed data)
# WARNING: keep this secret and never commit it. Format: 0x...
BACKEND_PRIVATE_KEY=0xYOUR_PRIVATE_KEY

# Supabase (or Postgres) credentials used by the server
SUPABASE_URL=https://your-supabase-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Rate limiting settings (optional)
CLAIM_RATE_LIMIT_WINDOW_MS=60000
CLAIM_RATE_LIMIT_MAX=6

# Logging
LOG_LEVEL=info

# Notes:
# - Replace placeholder values with your real keys before starting the server.
# - For CI or production, use a secure secret manager instead of committing a .env file.# =============================================================================
# OceanX Environment Variables (Backend)
# =============================================================================

# --- Supabase Configuration ---
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

# --- Server Configuration ---
PORT=10000
FRONTEND_URL=

# --- Blockchain Configuration (REQUIRED for marketplace & claims) ---

# RPC endpoint for the blockchain network
# Examples:
#   Base Sepolia Testnet: https://sepolia.base.org
#   Base Mainnet: https://mainnet.base.org
RPC_URL=

# Chain ID for the network
# Examples:
#   Base Sepolia: 84532
#   Base Mainnet: 8453
#   Ethereum Sepolia: 11155111
CHAIN_ID=

# OCXToken contract address (deployed on your network)
# This is the main token contract that handles claims
TOKEN_CONTRACT_ADDRESS=

# Backend signer private key
# ⚠️ CRITICAL: This MUST be the private key of the wallet set as 'authorizedSigner' 
#              in the OCXToken contract constructor during deployment
# ⚠️ NEVER commit this to version control! Keep it absolutely secret!
BACKEND_PRIVATE_KEY=

# (DEPRECATED - only needed if still using old OceanGameController)
# GAME_CONTRACT_ADDRESS=

# --- Security & Monitoring (OPTIONAL but RECOMMENDED for production) ---

# Webhook secret for claim-processed webhook authentication
# Generate with: openssl rand -hex 32
# ⚠️ REQUIRED in production - webhook calls are rejected without this
WEBHOOK_SECRET=

# Admin API key for accessing sensitive endpoints (/admin/sessions, etc.)
# Generate with: openssl rand -hex 32
# ⚠️ REQUIRED in production - admin endpoints return 403 without this
ADMIN_API_KEY=

# Sentry error monitoring DSN
# Get this from: https://sentry.io/settings/projects/your-project/keys/
# Example: https://abc123@o123456.ingest.sentry.io/7654321
SENTRY_DSN=

# Logging configuration
# Options: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# Notes:
# - Sentry captures errors, stack traces, and context for faster debugging
# - LOG_LEVEL=debug shows detailed logs (use 'info' in production)
# - In development, logs are pretty-printed with colors
# - In production, logs are JSON formatted for aggregation
# - WEBHOOK_SECRET and ADMIN_API_KEY are optional in development, required in production


