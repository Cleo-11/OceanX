// Minimal Diagnostics page so Next.js / Vercel build treats this as a valid module.
// Replace with the real diagnostics UI as needed.

export default function DiagnosticsPage() {
	return (
		<main>
			<h1>Diagnostics</h1>
			<p>This page exists to satisfy the app router module requirement.</p>
		</main>
	)
}
