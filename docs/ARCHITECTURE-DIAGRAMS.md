# 🎨 Secure Claim System - Architecture Diagrams

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         SECURE CLAIM SYSTEM                         │
│                                                                     │
│  Eliminates: Replay Attacks + Amount Forgery                       │
│  Using: EIP-712 Signatures + Atomic Transactions + Server Control  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Complete Flow Diagram

```
┌──────────────┐
│   Player     │ Completes action (mining, quest, etc.)
│   (Client)   │
└──────┬───────┘
       │ 1. "I finished mining!"
       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    YOUR GAME BACKEND                            │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ STEP 1: Calculate Rewards (SERVER-AUTHORITATIVE)         │  │
│  │                                                           │  │
│  │  const rewards = calculateMiningReward({                 │  │
│  │    miningPower: player.stats.miningPower,               │  │
│  │    timeElapsed: session.duration,                       │  │
│  │    difficulty: session.difficulty                       │  │
│  │  });                                                     │  │
│  │                                                           │  │
│  │  Client CANNOT influence this calculation!               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ STEP 2: Create Signature Record in Database             │  │
│  │                                                           │  │
│  │  INSERT INTO claim_signatures (                          │  │
│  │    claim_id,      // UUID (unique!)                      │  │
│  │    wallet,        // Player's address                    │  │
│  │    amount,        // Server-calculated amount            │  │
│  │    expires_at,    // Timestamp + 5 minutes               │  │
│  │    used           // false (not used yet)                │  │
│  │  )                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ STEP 3: Sign with EIP-712                                │  │
│  │                                                           │  │
│  │  payload = {                                             │  │
│  │    claimId: "550e8400-...",                              │  │
│  │    wallet: "0x742d35...",                                │  │
│  │    amount: "1500000000000000000",                        │  │
│  │    expiresAt: 1700000000                                 │  │
│  │  }                                                        │  │
│  │                                                           │  │
│  │  signature = wallet.signTypedData(domain, types, payload)│  │
│  │                                                           │  │
│  │  Using private key: CLAIM_SIGNER_PRIVATE_KEY             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ STEP 4: Return to Client                                 │  │
│  │                                                           │  │
│  │  return {                                                │  │
│  │    payload: { claimId, wallet, amount, expiresAt },     │  │
│  │    signature: "0xabcd1234..."                            │  │
│  │  }                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│   Player     │ Receives signed claim
│   (Client)   │ { payload, signature }
└──────┬───────┘
       │
       │ 5. Client submits claim to /api/claim
       │    POST { payload, signature }
       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    POST /api/claim                              │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ VALIDATION 1: Verify EIP-712 Signature                   │  │
│  │                                                           │  │
│  │  recoveredAddress = verifyTypedData(                     │  │
│  │    domain, types, payload, signature                     │  │
│  │  )                                                        │  │
│  │                                                           │  │
│  │  if (recoveredAddress != payload.wallet) REJECT          │  │
│  │                                                           │  │
│  │  ✅ Prevents: Signature forgery, payload tampering       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ VALIDATION 2: Check Expiration                           │  │
│  │                                                           │  │
│  │  now = Date.now() / 1000                                 │  │
│  │  if (payload.expiresAt < now) REJECT                     │  │
│  │                                                           │  │
│  │  ✅ Prevents: Stale signature reuse                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ DATABASE TRANSACTION (All or Nothing!)                   │  │
│  │                                                           │  │
│  │  BEGIN;                                                   │  │
│  │                                                           │  │
│  │    -- 1. LOCK THE ROW (prevent concurrent use)           │  │
│  │    SELECT * FROM claim_signatures                        │  │
│  │    WHERE claim_id = payload.claimId                      │  │
│  │    FOR UPDATE NOWAIT;                                    │  │
│  │                                                           │  │
│  │    -- 2. VERIFY NOT ALREADY USED                         │  │
│  │    if (row.used == true) {                               │  │
│  │      ROLLBACK; REJECT "Already used";                    │  │
│  │    }                                                      │  │
│  │                                                           │  │
│  │    -- 3. VERIFY WALLET MATCHES                           │  │
│  │    if (row.wallet != payload.wallet) {                   │  │
│  │      ROLLBACK; REJECT "Unauthorized";                    │  │
│  │    }                                                      │  │
│  │                                                           │  │
│  │    -- 4. VERIFY AMOUNT MATCHES (CRITICAL!)               │  │
│  │    if (row.amount != payload.amount) {                   │  │
│  │      ROLLBACK; REJECT "Amount mismatch";                 │  │
│  │    }                                                      │  │
│  │                                                           │  │
│  │    -- 5. MARK AS USED (prevent replay)                   │  │
│  │    UPDATE claim_signatures                               │  │
│  │    SET used = true, used_at = NOW()                      │  │
│  │    WHERE claim_id = payload.claimId;                     │  │
│  │                                                           │  │
│  │    -- 6. CREDIT PLAYER BALANCE                           │  │
│  │    UPDATE players                                        │  │
│  │    SET token_balance = token_balance + row.amount       │  │
│  │    WHERE wallet = payload.wallet;                        │  │
│  │                                                           │  │
│  │  COMMIT;                                                  │  │
│  │                                                           │  │
│  │  ✅ Prevents: Replay attacks, race conditions            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ SUCCESS RESPONSE                                         │  │
│  │                                                           │  │
│  │  return {                                                │  │
│  │    success: true,                                        │  │
│  │    claimId: "550e8400-...",                              │  │
│  │    amount: "1500000000000000000",                        │  │
│  │    newBalance: "5500000000000000000"                     │  │
│  │  }                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│   Player     │ ✅ Tokens successfully claimed!
│   (Client)   │ Balance updated
└──────────────┘
```

---

## Attack Prevention Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    ATTACK SCENARIOS                             │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ ATTACK 1: Replay Attack                                      │
│ Hacker tries to reuse the same signature multiple times     │
└──────────────────────────────────────────────────────────────┘

Legitimate claim:
  POST /api/claim { payload, signature }  ✅ SUCCESS (first use)
  
Replay attempt:
  POST /api/claim { same payload, same signature }  ❌ REJECTED
  
Why it fails:
  Database check: claim.used == true
  Error: "CLAIM_ALREADY_USED"

Prevention:
  ┌─────────────────────────────────────────┐
  │ Database tracks 'used' flag            │
  │ + Row-level locking prevents races     │
  │ + Atomic transaction ensures safety    │
  └─────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ ATTACK 2: Amount Forgery                                     │
│ Hacker tries to modify the claim amount                     │
└──────────────────────────────────────────────────────────────┘

Original claim (signed):
  amount: "1500000000000000000" (1.5 tokens)
  signature: "0xabcd1234..." (valid for 1.5 tokens)

Tampered claim:
  amount: "999999999999999999999" (999999 tokens!)
  signature: "0xabcd1234..." (same signature)

Why it fails:
  EIP-712 verification recovers wrong address
  Error: "INVALID_SIGNATURE"

Prevention:
  ┌─────────────────────────────────────────┐
  │ Signature is for ENTIRE payload         │
  │ + Changing amount invalidates signature │
  │ + Server also verifies amount vs DB     │
  └─────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ ATTACK 3: Race Condition                                     │
│ Multiple concurrent requests with same signature            │
└──────────────────────────────────────────────────────────────┘

Simultaneous requests:
  Request 1: POST /api/claim { payload, signature } 
  Request 2: POST /api/claim { same payload, same signature }
  Request 3: POST /api/claim { same payload, same signature }

What happens:
  ┌─────────────────────────────────────────┐
  │ Request 1: Acquires row lock             │
  │            ✅ Processes successfully     │
  │            Marks claim as used           │
  │            Releases lock                 │
  └─────────────────────────────────────────┘
  ┌─────────────────────────────────────────┐
  │ Request 2: Acquires lock (after #1)     │
  │            Sees claim.used = true        │
  │            ❌ Rejected immediately       │
  └─────────────────────────────────────────┘
  ┌─────────────────────────────────────────┐
  │ Request 3: Acquires lock (after #2)     │
  │            Sees claim.used = true        │
  │            ❌ Rejected immediately       │
  └─────────────────────────────────────────┘

Prevention:
  SELECT ... FOR UPDATE NOWAIT
  (Row-level locking + atomic transaction)

┌──────────────────────────────────────────────────────────────┐
│ ATTACK 4: Bypassing Signature (Direct DB manipulation)      │
│ Hacker tries to mark signature as unused or change amount   │
└──────────────────────────────────────────────────────────────┘

Attempt:
  UPDATE claim_signatures 
  SET used = false 
  WHERE claim_id = '...'

Why it fails:
  ┌─────────────────────────────────────────┐
  │ Row-Level Security (RLS) policies       │
  │ + Only service role can write           │
  │ + Client keys cannot modify             │
  │ + Database constraints enforce rules    │
  └─────────────────────────────────────────┘
```

---

## Security Layers Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    DEFENSE IN DEPTH                             │
│                                                                 │
│  Multiple independent security layers protect the system       │
└─────────────────────────────────────────────────────────────────┘

Client Request
    ↓
┌─────────────────────────────────────────────────────────────┐
│ LAYER 1: Input Validation                                   │
│                                                             │
│ ✓ Request format correct?                                   │
│ ✓ All required fields present?                              │
│ ✓ Field types valid?                                        │
│                                                             │
│ If fails: 400 Bad Request                                   │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ LAYER 2: Cryptographic Verification (EIP-712)              │
│                                                             │
│ ✓ Signature mathematically valid?                           │
│ ✓ Signer matches claim wallet?                              │
│ ✓ Payload hasn't been modified?                             │
│                                                             │
│ If fails: 401 Unauthorized (INVALID_SIGNATURE)              │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ LAYER 3: Temporal Validation                               │
│                                                             │
│ ✓ Signature not expired?                                    │
│ ✓ Clock drift tolerance applied                            │
│                                                             │
│ If fails: 410 Gone (SIGNATURE_EXPIRED)                      │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ LAYER 4: Database Existence Check                          │
│                                                             │
│ ✓ Claim ID exists in database?                              │
│ ✓ Record created by legitimate server?                      │
│                                                             │
│ If fails: 404 Not Found (CLAIM_NOT_FOUND)                   │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ LAYER 5: Row-Level Locking                                 │
│                                                             │
│ ✓ Acquire exclusive lock on claim row                       │
│ ✓ Prevents concurrent access                                │
│ ✓ Serializes all operations                                 │
│                                                             │
│ If fails: 409 Conflict (claim being processed)              │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ LAYER 6: Replay Check                                      │
│                                                             │
│ ✓ Signature not already used?                               │
│ ✓ used = false in database?                                 │
│                                                             │
│ If fails: 409 Conflict (CLAIM_ALREADY_USED)                 │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ LAYER 7: Authorization Check                               │
│                                                             │
│ ✓ Wallet in payload matches DB?                             │
│ ✓ Signature for correct wallet?                             │
│                                                             │
│ If fails: 403 Forbidden (UNAUTHORIZED_WALLET)               │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ LAYER 8: Amount Verification (Defense in Depth)            │
│                                                             │
│ ✓ Amount in payload matches DB?                             │
│ ✓ Server-calculated amount enforced?                        │
│ ✓ No client manipulation possible?                          │
│                                                             │
│ If fails: 400 Bad Request (AMOUNT_MISMATCH)                 │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ LAYER 9: State Updates (Atomic)                            │
│                                                             │
│ 1. Mark signature as used                                   │
│ 2. Record usage timestamp                                   │
│ 3. Credit player balance                                    │
│ 4. Commit transaction                                       │
│                                                             │
│ If any step fails: ROLLBACK all changes                     │
└─────────────────────────────────────────────────────────────┘
    ↓
✅ SUCCESS
```

---

## Data Flow Diagram

```
┌──────────────────────────────────────────────────────────────┐
│                    DATA FLOW                                 │
└──────────────────────────────────────────────────────────────┘

CLAIM CREATION (Server-Side Only)
──────────────────────────────────

    Game Logic
        │
        │ Player completes action
        ▼
    Calculate Reward
    ─────────────────
    │ Player stats   │──┐
    │ Time elapsed   │  │
    │ Difficulty     │  │ Server
    │ Bonuses        │  │ decides
    │ Game rules     │  │ amount
    └─────────────────  │
        │              │
        ▼              │
    Amount: "1500..." ◄─┘
        │
        ▼
    Create DB Record
    ─────────────────
    claim_id:   UUID
    wallet:     0x...
    amount:     1500... ◄── Server-set, locked
    expires_at: timestamp
    used:       false
        │
        ▼
    Sign with EIP-712
    ─────────────────
    Private key: CLAIM_SIGNER_PRIVATE_KEY
    Payload:     { claimId, wallet, amount, expiresAt }
    Output:      signature (0xabcd...)
        │
        ▼
    Send to Client
    ───────────────
    {
      payload:   { claimId, wallet, amount, expiresAt }
      signature: 0xabcd...
    }


CLAIM VERIFICATION (Client to Server)
──────────────────────────────────────

    Client receives claim
        │
        ▼
    Client submits to /api/claim
    POST { payload, signature }
        │
        ▼
    Verify Signature
    ─────────────────
    Input:  payload + signature
    Verify: EIP-712 domain + types
    Recover: signer address
    Check: signer == payload.wallet
        │
        ▼
    Database Lookup
    ────────────────
    SELECT * FROM claim_signatures
    WHERE claim_id = payload.claimId
    FOR UPDATE NOWAIT;
        │
        ▼
    Validate State
    ───────────────
    ✓ used = false
    ✓ expires_at > now
    ✓ wallet matches
    ✓ amount matches ◄── Critical check!
        │
        ▼
    Execute Transaction
    ────────────────────
    UPDATE claim_signatures
    SET used = true, used_at = NOW()
    
    UPDATE players
    SET token_balance = token_balance + amount
        │
        ▼
    Return to Client
    ─────────────────
    {
      success: true,
      amount: "1500...",
      newBalance: "5500..."
    }
```

---

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    COMPONENT INTERACTIONS                       │
└─────────────────────────────────────────────────────────────────┘

┌────────────────┐
│  Game Client   │
└────────┬───────┘
         │
         │ (1) Game action complete
         ▼
┌─────────────────────────┐
│  Game Backend API       │
│  /api/mining/complete   │
└────────┬────────────────┘
         │
         │ (2) Calculate rewards
         ▼
┌─────────────────────────┐
│  Reward Calculator      │
│  (Your game logic)      │
└────────┬────────────────┘
         │
         │ (3) Create claim
         ▼
┌─────────────────────────┐
│  Claim Generator        │
│  generateClaimSignature │
└────────┬────────────────┘
         │
         ├──(3a)──► Database: INSERT claim_signatures
         │
         └──(3b)──► Signer: EIP-712 signature
                            ▼
                    ┌──────────────────┐
                    │  Claim Signature │
                    │  {payload, sig}  │
                    └──────┬───────────┘
                           │
         ┌─────────────────┘
         │ (4) Return to client
         ▼
┌────────────────┐
│  Game Client   │
└────────┬───────┘
         │
         │ (5) Submit claim
         ▼
┌─────────────────────────┐
│  Claim API              │
│  POST /api/claim        │
└────────┬────────────────┘
         │
         ├──(6a)──► Signature Verifier: EIP-712 check
         │                              ▼
         │                      ┌──────────────┐
         │                      │ Valid? ✓/✗   │
         │                      └──────┬───────┘
         │                             │
         └──(6b)──────────────────────┘
         │
         │ (7) Process transaction
         ▼
┌─────────────────────────┐
│  Database Transaction   │
│  process_claim_tx()     │
└────────┬────────────────┘
         │
         ├──(7a)──► Lock claim row
         ├──(7b)──► Validate state
         ├──(7c)──► Mark as used
         ├──(7d)──► Update balance
         └──(7e)──► COMMIT
                    ▼
            ┌──────────────┐
            │  Success ✅  │
            └──────┬───────┘
                   │
         ┌─────────┘
         │ (8) Response
         ▼
┌────────────────┐
│  Game Client   │ Balance updated!
└────────────────┘
```

---

**Use these diagrams to understand the system architecture and security model! 🎨**
